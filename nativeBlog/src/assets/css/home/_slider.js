import { StyleSheet } from 'react-native';
 import { width } from '../../../utilities/size';

export const styles = StyleSheet.create({
   container: { 
     flex: 1, 
     backgroundColor: 'white' 
   },
   child: { 
     width,
    },
   text: {
     fontSize: 30, 
     textAlign: 'center' 
   },
 });